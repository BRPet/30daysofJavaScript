let variable1, // not defined, because it is not assigned to a value yet
    variable2, // not defined, because it is not assigned to a value yet
    variable3, // not defined, because it is not assigned to a value yet
    variable4; // not defined, because it is not assigned to a value yet
console.log(variable1, variable2, variable3, variable4);
